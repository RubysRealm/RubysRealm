name: Cross-channel media copying disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Preserve separate channel pipelines
        run: |
          echo "Cross-channel media copying is disabled."
          echo "Each account must publish only from its own assigned content pipeline."
