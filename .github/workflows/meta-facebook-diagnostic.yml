name: Meta Facebook permission diagnostic

on:
  workflow_dispatch:
  push:
    paths:
      - 'viral_facebook/meta_diag.trigger'

permissions:
  contents: read

jobs:
  diagnose:
    runs-on: ubuntu-latest
    steps:
      - name: Inspect token app and granted permissions
        env:
          FACEBOOK_PAGE_ACCESS_TOKEN: ${{ secrets.FACEBOOK_PAGE_ACCESS_TOKEN }}
        run: |
          set -euo pipefail
          echo 'APP:'
          curl -sS -G \
            --data-urlencode 'fields=id,name' \
            --data-urlencode "access_token=${FACEBOOK_PAGE_ACCESS_TOKEN}" \
            'https://graph.facebook.com/v26.0/app' || true
          echo
          echo 'PERMISSIONS:'
          curl -sS -G \
            --data-urlencode "access_token=${FACEBOOK_PAGE_ACCESS_TOKEN}" \
            'https://graph.facebook.com/v26.0/me/permissions' || true
          echo
