name: RubyClips legacy source disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Legacy YouTube/creator-source publishing is disabled. RubyClips now uses the MuffinDrama TikTok story pipeline.'
