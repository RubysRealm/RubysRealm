{
  "name": "rubys-realm",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rm -rf public && mkdir -p public && cp index.html privacy.html terms.html ready.html public/"
  },
  "dependencies": {
    "ffmpeg-static": "^5.2.0",
    "node-edge-tts": "^1.2.10",
    "sharp": "^0.34.3",
    "jose": "^6.1.0",
    "@vercel/oidc": "^3.0.5"
  }
}
