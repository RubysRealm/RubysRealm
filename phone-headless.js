import express from 'express';
import { chromium } from 'playwright-core';
import { spawn, spawnSync } from 'child_process';
import fs from 'fs';
import path from 'path';
import ffmpegPath from 'ffmpeg-static';

const PORT=Number(process.env.PORT||10000), TOKEN=String(process.env.DESKTOP_TOKEN||'').trim();
const VIDEO_ID=process.env.YOUTUBE_VIDEO_ID||'5-bO9NAhWbI', VIDEO_URL=process.env.YOUTUBE_SOURCE_URL||`https://www.youtube.com/watch?v=${VIDEO_ID}`;
const TOTAL_PARTS=Number(process.env.STORY_TOTAL_PARTS||11), SOURCE_DURATION=Number(process.env.SOURCE_DURATION_SECONDS||6372), SEGMENT_SECONDS=Number(process.env.SEGMENT_SECONDS||595);
const PROFILE='/tmp/youtube-browser', SOURCE_DIR='/tmp/rubyclips-source', STATUS=path.join(SOURCE_DIR,'status.json'), FULL=path.join(SOURCE_DIR,'full.mp4');
const CHROME=process.env.CHROME_PATH||path.resolve('.chrome/opt/google/chrome/google-chrome'), YTDLP=process.env.YTDLP_PATH||path.resolve('yt-dlp');
fs.mkdirSync(PROFILE,{recursive:true}); fs.mkdirSync(SOURCE_DIR,{recursive:true});
let page=null, context=null, acquisitionStarted=false;

function readyParts(){const a=[];for(let i=1;i<=TOTAL_PARTS;i++){const f=path.join(SOURCE_DIR,`source-part-${String(i).padStart(2,'0')}.mp4`);if(fs.existsSync(f)&&fs.statSync(f).size>250000)a.push(i);}return a;}
function setStatus(extra={}){let p={};try{p=JSON.parse(fs.readFileSync(STATUS,'utf8'));}catch{}const s={stage:'starting_browser',signedIn:false,downloading:false,videoId:VIDEO_ID,sourceUrl:VIDEO_URL,totalParts:TOTAL_PARTS,sourceDurationSeconds:SOURCE_DURATION,segmentSeconds:SEGMENT_SECONDS,...p,...extra,readyParts:readyParts(),updatedAt:new Date().toISOString()};fs.writeFileSync(STATUS,JSON.stringify(s,null,2));return s;}
function getStatus(){try{return JSON.parse(fs.readFileSync(STATUS,'utf8'));}catch{return setStatus();}}
function run(cmd,args){return new Promise((resolve,reject)=>{const p=spawn(cmd,args,{stdio:['ignore','inherit','inherit']});p.once('error',reject);p.once('exit',c=>c===0?resolve():reject(new Error(`${path.basename(cmd)} exited ${c}`)));});}
function probeLogin(){const r=spawnSync(YTDLP,['--ffmpeg-location',ffmpegPath,'--cookies-from-browser',`chrome+basictext:${PROFILE}`,'--no-playlist','--skip-download','--no-warnings','--print','%(id)s',VIDEO_URL],{encoding:'utf8',timeout:90000});return r.status===0&&String(r.stdout||'').trim().split(/\s+/).includes(VIDEO_ID);}
async function acquire(){setStatus({stage:'downloading_source',signedIn:true,downloading:true,error:null});await run(YTDLP,['--ffmpeg-location',ffmpegPath,'--cookies-from-browser',`chrome+basictext:${PROFILE}`,'--no-playlist','--retries','20','--fragment-retries','20','--concurrent-fragments','4','-f','bv*[height<=720][vcodec^=avc1]+ba[acodec^=mp4a]/b[height<=720][ext=mp4]/bv*[height<=720]+ba/b[height<=720]','--merge-output-format','mp4','--remux-video','mp4','-o',path.join(SOURCE_DIR,'full.%(ext)s'),VIDEO_URL]);const files=fs.readdirSync(SOURCE_DIR).filter(x=>/^full\.(mp4|mkv|webm|mov)$/i.test(x)).map(x=>path.join(SOURCE_DIR,x));if(!files.length)throw new Error('No downloaded source file was created');const src=files.sort((a,b)=>fs.statSync(b).size-fs.statSync(a).size)[0];if(src!==FULL)await run(ffmpegPath,['-y','-hide_banner','-loglevel','error','-i',src,'-c','copy','-movflags','+faststart',FULL]);if(!fs.existsSync(FULL)||fs.statSync(FULL).size<1000000)throw new Error('Downloaded source is unexpectedly small');setStatus({stage:'splitting_source',signedIn:true,downloading:true});for(let part=1;part<=TOTAL_PARTS;part++){const out=path.join(SOURCE_DIR,`source-part-${String(part).padStart(2,'0')}.mp4`),start=(part-1)*SEGMENT_SECONDS,len=Math.min(SEGMENT_SECONDS,Math.max(0,SOURCE_DURATION-start));if(len<=0.5)break;if(!(fs.existsSync(out)&&fs.statSync(out).size>250000)){const tmp=out+'.tmp.mp4';try{fs.unlinkSync(tmp);}catch{}await run(ffmpegPath,['-y','-hide_banner','-loglevel','error','-ss',String(start),'-i',FULL,'-t',String(len),'-map','0:v:0','-map','0:a:0?','-c','copy','-avoid_negative_ts','make_zero','-movflags','+faststart',tmp]);fs.renameSync(tmp,out);}setStatus({stage:`part_${part}_ready`,signedIn:true,downloading:true});}fs.writeFileSync(path.join(SOURCE_DIR,'source-manifest.json'),JSON.stringify({sourceProvider:'youtube-auth-browser',sourceChannel:'@muffindrama-uvu',videoId:VIDEO_ID,sourceUrl:VIDEO_URL,durationSeconds:SOURCE_DURATION,totalParts:TOTAL_PARTS,segmentSeconds:SEGMENT_SECONDS,sessionExported:false,createdAt:new Date().toISOString()},null,2));setStatus({stage:'ready',signedIn:true,downloading:false,error:null});}

const app=express();app.use(express.json({limit:'64kb'}));app.use(express.static('public'));
function auth(req,res,next){const t=String(req.query.token||req.headers['x-desktop-token']||'');if(!TOKEN||t!==TOKEN)return res.status(403).send('Bad desktop token');next();}
app.get('/',(req,res)=>{const s=getStatus();res.type('html').send(`<meta name="viewport" content="width=device-width,initial-scale=1"><body style="background:#090a0f;color:#fff;font:18px system-ui;padding:28px"><h1>Rubradaclips YouTube Source</h1><p>Status: <b>${s.stage}</b></p><p>Ready parts: ${(s.readyParts||[]).length}/${s.totalParts}</p></body>`);});
app.get('/desktop',auth,(req,res)=>res.sendFile(path.resolve('public/desktop.html')));
app.get('/api/desktop-frame',auth,async(req,res)=>{if(!page)return res.status(503).send('Browser starting');try{const b=await page.screenshot({type:'jpeg',quality:72});res.setHeader('Content-Type','image/jpeg');res.setHeader('Cache-Control','no-store');res.end(b);}catch(e){res.status(503).send(e.message);}});
app.post('/api/desktop-click',auth,async(req,res)=>{if(!page)return res.status(503).json({ok:false});try{await page.mouse.click(Math.max(0,Math.min(719,Number(req.body?.x)||0)),Math.max(0,Math.min(1279,Number(req.body?.y)||0)));res.json({ok:true});}catch(e){res.status(500).json({ok:false,error:e.message});}});
app.post('/api/desktop-type',auth,async(req,res)=>{if(!page)return res.status(503).json({ok:false});try{await page.keyboard.type(String(req.body?.text||'').slice(0,500),{delay:20});res.json({ok:true});}catch(e){res.status(500).json({ok:false,error:e.message});}});
app.post('/api/desktop-key',auth,async(req,res)=>{if(!page)return res.status(503).json({ok:false});const m={Return:'Enter',Tab:'Tab',Escape:'Escape',BackSpace:'Backspace',Up:'ArrowUp',Down:'ArrowDown',Left:'ArrowLeft',Right:'ArrowRight',space:'Space','ctrl+l':'Control+L'},k=m[String(req.body?.key||'')];if(!k)return res.status(400).json({ok:false});try{await page.keyboard.press(k);res.json({ok:true});}catch(e){res.status(500).json({ok:false,error:e.message});}});
app.get('/api/state',(req,res)=>res.json(getStatus()));
app.get('/source/:name',(req,res)=>{const n=path.basename(String(req.params.name||''));if(!/^source-part-\d{2}\.mp4$/.test(n)&&n!=='source-manifest.json')return res.status(404).end();const f=path.join(SOURCE_DIR,n);if(!fs.existsSync(f))return res.status(404).end();res.setHeader('Cache-Control','no-store');res.sendFile(f);});
app.get('/healthz',(req,res)=>res.json({ok:true,service:'rubyclips-youtube-phone',browser:!!page}));
app.listen(PORT,'0.0.0.0',()=>console.log(`Rubradaclips YouTube phone listening on ${PORT}`));

async function boot(){setStatus({stage:'starting_browser',signedIn:false,downloading:false,error:null});try{context=await chromium.launchPersistentContext(PROFILE,{headless:true,executablePath:CHROME,viewport:{width:720,height:1280},args:['--no-sandbox','--disable-dev-shm-usage','--password-store=basic','--no-first-run','--no-default-browser-check','--disable-features=TranslateUI','--disable-blink-features=AutomationControlled']});page=context.pages()[0]||await context.newPage();await page.goto(VIDEO_URL,{waitUntil:'domcontentloaded',timeout:60000}).catch(e=>console.log('initial navigation:',e.message));setStatus({stage:'waiting_for_login',signedIn:false,downloading:false,error:null});setInterval(async()=>{if(acquisitionStarted||getStatus().stage==='ready')return;if(!probeLogin())return;acquisitionStarted=true;try{await acquire();}catch(e){console.error(e);setStatus({stage:'error',signedIn:true,downloading:false,error:String(e?.message||e)});acquisitionStarted=false;}},6000).unref();}catch(e){console.error('browser startup failed',e);setStatus({stage:'browser_error',error:String(e?.message||e)});}}
boot();
